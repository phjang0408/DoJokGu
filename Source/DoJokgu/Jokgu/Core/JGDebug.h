#pragma once

#include "CoreMinimal.h"
#include "HAL/IConsoleManager.h"

/** jg.Debug.Draw 1 : draws court zones, hit reach, ball trajectory and ground contacts (development only, never a game HUD feature) */
extern TAutoConsoleVariable<bool> CVarJGDrawDebug;
